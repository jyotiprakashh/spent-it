Icon-only control for headers and toolbars. Quiet by default; \`tinted\` for emphasis.

\`\`\`jsx
<IconButton icon="search" aria-label="Search" onClick={open} />
<IconButton icon="add" tinted aria-label="Add" />
\`\`\`
