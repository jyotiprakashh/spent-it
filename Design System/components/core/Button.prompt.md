The primary action control. Filled green \`primary\`, neutral \`secondary\`, quiet \`ghost\`, red \`danger\`. Presses shrink to 0.97.

\`\`\`jsx
<Button variant="primary" leftIcon="add" onClick={save}>Add transaction</Button>
<Button variant="secondary" size="sm">Cancel</Button>
\`\`\`

Use one primary per view. \`fullWidth\` for sheet/CTA footers.
