Inline pill toggle for 2–3 options (Expense/Income, ranges).

\`\`\`jsx
<SegmentedControl value={type} onChange={setType}
options={[{value:'expense',label:'Expense',tone:'expense'},{value:'income',label:'Income',tone:'income'}]} />
\`\`\`
